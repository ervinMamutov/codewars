'use strict';

const uniqueInOrder = (iterable) => {
  //your code here - remember iterable can be a string or an array
  iterable.map((char) => charCodeAt(char));
};

require('./test.js')(uniqueInOrder);
